import { PicturepathPipe } from './picturepath.pipe';

describe('PicturepathPipe', () => {
  it('create an instance', () => {
    const pipe = new PicturepathPipe();
    expect(pipe).toBeTruthy();
  });
});
