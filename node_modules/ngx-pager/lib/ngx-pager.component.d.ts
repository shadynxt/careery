import { OnChanges, SimpleChanges } from '@angular/core';
import { Router, ActivatedRoute } from '@angular/router';
export declare class NgxPagerComponent implements OnChanges {
    private route;
    private activeRoute;
    config: any;
    totalrow: any;
    pages: Array<any>;
    currentPage: number;
    displayPagesCount: number;
    totalPage: number;
    simple: boolean;
    firstLastView: boolean;
    error: boolean;
    redirectType: string;
    redirectParamName: string;
    customContent: any;
    constructor(route: Router, activeRoute: ActivatedRoute);
    ngOnInit(): boolean;
    ngOnChanges(changes: SimpleChanges): void;
    settings(): boolean;
    pageClick(event: any, pageNo: any): boolean;
    redefinePager(): void;
    rendering(): void;
}
